# 镜像仓库坐标（非机密，随仓库提交）。
#
# 访问凭证不在此处：请用 `podman login` / `docker login` 写入引擎自己的凭证文件
# （~/.config/containers/auth.json 或 ~/.docker/config.json，0600），或由 CI 的 secret 注入。
#
# 赋值使用 := 只在未设置时生效，因此环境变量与命令行参数（--registry / --namespace）
# 的优先级高于本文件。
: "${BANQI_REGISTRY:=registry.cn-hangzhou.aliyuncs.com}"
: "${BANQI_REGISTRY_NAMESPACE:=banqi}"
